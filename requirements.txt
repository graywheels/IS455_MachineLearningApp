# Python dependencies for the ML pipeline (jobs/ + model_training.ipynb)
# Install with:  pip install -r requirements.txt

pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
joblib>=1.3
matplotlib>=3.8
seaborn>=0.13

# Standard library additions (zoneinfo is built-in on Python 3.9+)
# On Python 3.8:  pip install backports.zoneinfo
